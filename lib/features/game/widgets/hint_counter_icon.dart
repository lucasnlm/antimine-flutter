import 'package:flutter/material.dart';

class HintCounterIcon extends StatelessWidget {
  const HintCounterIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.auto_fix_high);
  }
}
