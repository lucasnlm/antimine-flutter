import 'package:flame/game.dart';

class ComponentConstants {
  static const double spriteSize = 256;
  static const double extraSpace = 4.0 / 256;
  static const double extraSpace2 = 2.0 / 256;
  static final Vector2 srcSize = Vector2(spriteSize, spriteSize);
  static const int iconPadding = 8;
  static const int mineBackgroundPadding = 2;
}
